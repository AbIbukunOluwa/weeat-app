<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>My Profile — WeEat</title>
  <link rel="stylesheet" href="/public/css/styles.css">
</head>
<body>
<header>
  <h1>WeEat</h1>
  <nav>
    <a href="/">Home</a> |
    <a href="/menu">Menu</a> |
    <a href="/about">About</a> |
    <% if (currentUser) { %>
      <form style="display:inline" method="post" action="/auth/logout"><button>Logout</button></form>
    <% } else { %>
      <a href="/auth/login">Login</a>
    <% } %>
  </nav>
</header>
<main>
  <h2>Welcome, <%= currentUser ? currentUser.name : 'Guest' %></h2>
  <p>Email: <%= currentUser ? currentUser.email : '' %></p>
</main>
</body>
</html>
